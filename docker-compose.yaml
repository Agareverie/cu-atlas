services:
  mongodb:
    image: mongo:8
    container_name: cu-atlas-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data: